App\Entity\Army:
    type: entity
    id:
        id:
            type: integer
            generator:
                strategy: AUTO
    manyToOne:
        war:
            targetEntity: War
            inversedBy: armies
    fields:
        tag:
            type: string
            length: 50
            nullable: false